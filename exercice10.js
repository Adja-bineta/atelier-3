let codePostal = 77000; // Premier code postal du département 77
const limiteSuperieure = 77999; // Limite supérieure des codes postaux du département 77

while (codePostal <= limiteSuperieure) {
  console.log(codePostal);
  codePostal++;
}